package org.exp.incaskbot.model.enums;

/**
 * UriType — URI (URL)larning turini bildiradi.

 * - MAIN: asosiy, o‘zgarmas va doimiy URI.
 * - PERMANENT: davomiy, lekin o‘zgartirilishi mumkin bo‘lgan URI.
 * - TEMPORARY: vaqtinchalik va o‘zgartirib bo‘lmaydigan URI.
 */
public enum UriType {
    MAIN,
    PERMANENT,
    TEMPORARY
}
